{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "yarn start",
    "local": "env $(cat .env | grep -v '^#' | xargs) nodemon server.js"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "express": "^4.17.1",
    "firebase": "^7.14.2"
  }
}
